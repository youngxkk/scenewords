import Foundation

enum DeveloperAPISecrets {
    // Developer-only fallback config.
    // If Info.plist / environment values are unavailable, the app uses these.
    static let apiBaseURL: String = "https://scenewords-api.youngxkk.workers.dev"
    static let apiToken: String = "f14c83cd4cb607cebac84b22bf8133013e1b9ec3d15dda248616574cc40794cb"
}
